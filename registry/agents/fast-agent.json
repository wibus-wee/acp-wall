{
  "name": "fast-agent",
  "display": "fast-agent",
  "vendor": "enquiries@fast-agent.ai",
  "version": "0.10.1",
  "description": "Code and build agents with comprehensive multi-provider support",
  "repo": "evalstate/fast-agent",
  "website": "https://fast-agent.ai",
  "icon": "https://cdn.agentclientprotocol.com/registry/v1/latest/fast-agent.svg",
  "license": "Apache 2.0",
  "install": "node tools/install-dist.mjs registry/agents/fast-agent.json",
  "run": "uvx fast-agent-acp==0.10.1 -x",
  "env": {
    "OPENAI_API_KEY": "probe-mock-key",
    "OPENAI_BASE_URL": "${MOCK_LLM_URL}"
  },
  "sessionFiles": {
    ".fast-agent/fast-agent.yaml": "default_model: openai.probe-model\nopenai:\n  api_key: probe-mock-key\n  base_url: ${MOCK_LLM_URL}\n"
  },
  "dist": {
    "type": "uvx",
    "package": "fast-agent-acp==0.10.1",
    "args": [
      "-x"
    ]
  },
  "disabled": false,
  "selftest": false,
  "notes": "fast-agent reads fast-agent.yaml from ./.fast-agent relative to the session cwd (probe workspace); OPENAI_* env is the documented override. Model pinned to the openai chat-completions provider — no account needed.",
  "_synced": "acp-registry 1.0.0 · 2026-09-21"
}
