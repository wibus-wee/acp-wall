{
  "run": "npx -y @minimax-ai/code@0.2.7 provider add --name probe --base-url ${MOCK_LLM_URL} --model probe-model --api-key probe-mock-key --yes ; npx -y @minimax-ai/code@0.2.7 acp",
  "env": {
    "MINIMAX_API_KEY": "probe-mock-key"
  },
  "notes": "Attempted wiring: `mcode provider add` (documented CLI) registers an OpenAI-compatible provider pointed at the mock LLM before `acp` starts. The --api-key flag is inferred from the documented --name/--base-url/--model form; MINIMAX_API_KEY env covers the MiniMax-native path."
}
