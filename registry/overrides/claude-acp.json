{
  "env": {
    "ANTHROPIC_BASE_URL": "${MOCK_LLM_URL}",
    "ANTHROPIC_API_KEY": "probe-mock-key",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": "1"
  },
  "notes": "Official ACP adapter wrapping the Claude Agent SDK. Verified locally: claude honors ANTHROPIC_BASE_URL and completes full tool_use→end_turn turns against the mock's /messages SSE endpoint — the earlier failure was mock-side (query-string routing + model-name mismatch), not auth."
}
